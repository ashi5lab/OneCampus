<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required on Android 13+ (API 33, this app targets 35) for any
         notification to ever be shown, including FCM push. Capacitor's
         push-notifications plugin requests this permission at runtime via
         @capacitor/push-notifications' PUSH_NOTIFICATIONS permission alias,
         but per Capacitor's plugin permission model the app itself must
         still declare the underlying OS permission here — the plugin does
         not inject it into the app's manifest automatically. Without this
         line, Android silently auto-denies every request for it (no
         dialog is ever shown to the user, on any install), so push
         notifications can never display regardless of a valid FCM token
         or a granted app-level "notifications" toggle. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
