# Attendance Feature (Frontend)

**Purpose**: Read-only list of attendance records for now. Marking attendance (`POST /api/v1/attendance`) isn't wired up in the UI yet — it needs a per-cohort roster view to be useful, which is follow-up work.

**API Endpoints used**: `GET /api/v1/attendance`.
